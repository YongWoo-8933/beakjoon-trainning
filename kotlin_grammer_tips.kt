// 빠른 입력받기 
val a = readln()

// 공백으로 입력된 값 받기
val (x, y) = readln().split(" ").map { it.toInt() }

// n회 반복 간편하게
repeat(n) {

}

// 배열 메소드
lst.sumOf{ it.toInt() }
lst.minOf{ it.toInt() }
lst.maxOf{ it.toInt() }